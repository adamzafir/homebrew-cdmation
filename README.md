# homebrew-cdmation

Homebrew tap for [cdmation](https://github.com/adamzafir/cdmation).

## Install

```bash
brew tap adamzafir/cdmation
brew install cdmation
```

Then add the zsh hook:

```bash
echo 'source "$(brew --prefix)/opt/cdmation/share/cdmation-hook.zsh"' >> ~/.zshrc
source ~/.zshrc
```
